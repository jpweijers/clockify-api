name: CI
on: [push]
jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: install poetry
        run: |
          pip install poetry
          poetry install
          poetry shell
      - name: Run Tests
        run: |
          coverage run -m pytest
